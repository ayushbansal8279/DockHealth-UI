import styled from 'styled-components';
import { ModalWrapper } from '../styled';

export const CreateListModalWrapper = styled(ModalWrapper)`
  width: 530px;
`;

export default CreateListModalWrapper;
