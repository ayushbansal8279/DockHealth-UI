diff a/src/app/components/task/AddTask.jsx b/src/app/components/task/AddTask.jsx	(rejected hunks)
@@ -155,3 +155,3 @@ const mapDispatchToProps = (dispatch, { taskListId, patientId }) => ({
-    if(patientId && patientId>0){
-      saveTask({ description, taskListId, patientId })(dispatch)
-    }else{
+    if (patientId && patientId > 0) {
+      saveTask({ description, taskListId, patientId })(dispatch);
+    } else {
