import styled from 'styled-components';
import palette from 'styles/palette';

export const MentionItem = styled.span`
  background-color: rgba(239, 138, 35, 0.07);
  color: ${palette.brightBlue};
  cursor: pointer;
`;
