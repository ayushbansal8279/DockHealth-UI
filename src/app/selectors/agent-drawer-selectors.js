export const agentDrawerOpenSelector = (state) => state.agentDrawer?.open || false;
export const agentDrawerAgentTypeSelector = (state) => state.agentDrawer?.agentType || null;
export const agentDrawerTaskDataSelector = (state) => state.agentDrawer?.taskData || null;
export const agentDrawerUpdatedDataSelector = (state) => state.agentDrawer?.updatedData || null;
