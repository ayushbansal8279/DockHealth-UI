import { Typography } from '@mui/material';
import React from 'react';

const EmptyContent = () => {
  return (
    <Typography color="secondary">
      There's no Api Keys created, please create it.
    </Typography>
  );
};

export default EmptyContent;
