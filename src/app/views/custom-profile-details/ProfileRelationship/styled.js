import styled from "styled-components";
import { Box } from "@mui/material";

export const ProfileRelationsContainer = styled(Box)({
    height: 'calc(100vh - 260px)',
    display: 'flex',
    flexDirection: 'column',
    marginBottom: -48,
});